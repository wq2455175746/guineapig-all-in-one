{
  "service": "guineapig-client",
  "task_id": "client-command-robustness-and-whitelist-ui",
  "task_name": "Client 命令执行健壮性修复 + 命令白名单管理 UI",
  "status": "in_progress",
  "started_at": "2026-09-24",
  "description": "修复 execute-command 误拒合法命令（非 ASCII 参数/路径限定二进制），修复 fetch-mcp-resources 的 ESM __require 崩溃（SDK 子路径 external），并为命令白名单（DEFAULT_ALLOWED_COMMAND_BINARIES）新增系统设置页管理 UI（IPC + sanitize + 落盘 userData + 立即生效）",
  "milestones": {
    "execute-command 误拒修复（SAFE_ARG_TOKEN 非 ASCII + isAllowedBinary basename）": "2026-09-24",
    "fetch-mcp-resources ESM 崩溃修复（external 正则 ^/@modelcontextprotocol/sdk/）": "2026-09-24",
    "命令白名单管理 UI（SystemSettingsPage Tab + CommandWhitelistTab.vue + IPC 三步走）": "2026-09-24",
    "harness 更新（lessons L051-L053 / AP-033 / SP-039 / client AGENTS.md 重写）": "2026-09-24"
  },
  "completed_tasks": [
    "SAFE_ARG_TOKEN 改为 `(?:[A-Za-z0-9_./:@+=~-]|[\\u{0080}-\\u{10FFFF}])+`，允许中文等非 ASCII 参数，仍拒绝 shell 元字符",
    "isAllowedBinary 按 basename 匹配白名单，放行 /usr/bin/open 等路径限定二进制",
    "vite.config external 精确字符串 ':@modelcontextprotocol/sdk' 改正则 /^@modelcontextprotocol\\/sdk/，SDK 子路径保持 external",
    "清理 dist/electron 残留 SDK chunk（该 outDir 不自动清空）",
    "新增 get-command-whitelist / set-command-whitelist IPC + sanitizeCommandBinaries / persistCommandWhitelist（空列表回退默认）",
    "preload 暴露 getCommandWhitelist/setCommandWhitelist + vite-env.d.ts 类型扩展",
    "SystemSettingsPage 新增 命令白名单 Tab + CommandWhitelistTab.vue（每行一个命令名 + 保存/恢复默认）",
    "README 命令白名单节新增页面配置说明",
    "验证：sanitize 逻辑 node 用例 + vue-tsc + npm run build + make validate 全部通过"
  ],
  "next_tasks": [
    "真机运行验证：系统设置→命令白名单 保存后执行新命令即时生效",
    "重启应用后白名单从 command-whitelist.json 正确加载（含页面保存过的自定义项）"
  ],
  "updated_at": "2026-09-24T11:10:00"
}